{
  "name": "the-house-frontend",
  "private": true,
  "version": "0.8.0-phase3",
  "type": "module",
  "scripts": {
    "generate": "node scripts/generate-pages.mjs && node scripts/enhance-premium.mjs && node scripts/enhance-reference.mjs && node scripts/apply-pixel-pass.mjs && node scripts/apply-canonical-shell.mjs && node scripts/apply-canonical-hero.mjs && node scripts/apply-canonical-pulse.mjs && node scripts/apply-demo-noindex.mjs && tsc --outDir public/assets --noEmit false",
    "dev": "npm run generate && vite",
    "build": "npm run generate && node scripts/seo-check.mjs && node scripts/link-check.mjs && vite build",
    "typecheck": "tsc --noEmit",
    "seo:check": "node scripts/seo-check.mjs",
    "check": "npm run generate && npm run seo:check && npm run link:check",
    "link:check": "node scripts/link-check.mjs",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vite": "^8.1.0",
    "wrangler": "^4.120.1"
  }
}
